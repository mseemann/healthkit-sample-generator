health authorisierung anfordern

export für healtdaten - json format
- alle
- der app
- alle vom testcode erzeugten
- test für export HKPreditcate
- export als ? mail, speichern unter - aber wohin?, upload auf einen sever?

- url scheme um profil direkt zu laden, mit der generator app und dann den import zu starten